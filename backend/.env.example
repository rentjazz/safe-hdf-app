# Configuration Google OAuth2 pour Google Calendar
# Remplacez ces valeurs par vos propres credentials

GOOGLE_CLIENT_ID=votre_client_id_ici
GOOGLE_CLIENT_SECRET=votre_client_secret_ici
GOOGLE_REDIRECT_URI=http://localhost:8000/calendar/callback

# Base de données (optionnel - défaut: sqlite:///./safe_hdf.db)
# DATABASE_URL=sqlite:///data/safe_hdf.db